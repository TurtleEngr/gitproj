gitproj is a git subsystem for managing large binary files.
Its setup and management is simpler than "git LFS".

User help
	man gitproj
    or
	git proj -h

SubCmd help
	git proj SUBCMD -h | less

For a new local git repo, start here:
	git proj init -h | less

Internal Documentation
	man 8 giproj

Source
	git clone git@github.com:TurtleEngr/gitproj.git
